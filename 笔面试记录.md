4.23面试
什么是tcp/ip
http在哪一层


4.24 笔试
css选择器优先级
this指向，闭包
Promise（resolve，reject）
Math内置对象应用（random，floor）
数组排序（数组里包含对象）
vue组件相关（题目没时间细看，只知道是vue组件相关）


5.7 字节跳动面试（一面）应该是凉了
从输入url到浏览器页面渲染过程
tcp属于哪一层
计算机网络层级模型
dns具体实现
浏览器缓存
sessionStorage和localStorage区别
vue父子组件传值
vue兄弟组件传值
css3盒子模型和传统盒子模型（包括margin吗？）
this指向问答题
编程题：最大和的连续子数组
编程题：判断链表是否有环（之前做过的都不会做了，唉）
编程题都讲了一点思路，但是代码都没写起。。。


5.12 滴滴出行一面
自我介绍
描述之前的测试工作
之前的测试工作对前端开发有什么帮助吗
怎么学习web前端的
浏览器渲染页面过程
vue组件传值
vue 自定义指令
常用的vue指令
自己做的vue项目有遇到问题吗？怎么解决的？
了解跨域吗
怎么解决页面适配布局问题
自己未来的职业规划
编程题：找出一串字符串中最大的值，在后面加上标志
编程题：函数柯里化（闭包）实现fn(x)(y)(z)返回x+y+z;

5.21 滴滴出行二面
说工作经验不符合要求

5.20商汤科技一面
自我介绍
介绍自己做的项目，遇到的问题
对页面适配的看法（面试官说平时开发照着设计稿做就行，打包阶段可以一次性处理）
了解webpack吗（面试官说作为前端是必须了解的）
html语义化
盒子模型（传统的和css3盒子模型）
元素垂直居中方法
常用的定位种类
元素加了绝对定位会怎样（脱标）
js基本数据类型有哪些
怎么判断js数据类型
如何判断函数和对象数据类型（通过原型来判断-prototype.toString.call()来判断）
var let const区别
了解跨域吗
什么是任务队列（js事件机制）
常见的异步任务有哪些（Promise，定时器，事件绑定，异步请求，回调函数）
宏任务和微任务执行顺序，Promise什么时候执行
了解Promise.all()方法吗，应用场景（处理多个异步处理）
Promise的应用场景
Promise有几种状态（3种 pending初始状态  fulfilled操作成功  rejected操作失败）
闭包及其作用，应用场景（实现私有变量）
了解浏览器缓存吗
vue双向数据绑定原理
vue组件传值
vue组件的data为什么必须是一个函数
computed和watch区别
了解vue路由钩子吗

5.25 商汤二面（只记录不会的）
instance of（检测构造函数的prototype是否出现在某个实例对象的原型链上）判断某个对象是否属于某个构造函数
querySelectorAll()和getElementsByClassName()返回结果
querySelectorAll返回类型是NodeList，getElementsByClassName返回类型是HTMLCollection
vue diff算法
react组件为什么要继承super(props)
继承父类的构造函数，得到父类的实例属性和方法，从而塑造自己的this，如果不调用super，就得不到this
精度丢失问题怎么解决
可使用toFixed(num) num是精确到第几位
vue 编译
解析模板》生成ast抽象语法树》将ast转换为可执行代码
vue 权限控制
vue hash模式和history模式了解（哈希模式是默认模式，有“#”号，history模式需要额外设置，没有“#”号）
对webpack的了解
axios除了发送请求的其他用途
原型方法

5.25 商汤三面
vue什么时候只能用computed（路由和异步请求），什么时候只能用watch
编程题 两数之和
前后端传输大容量文件 怎么解决


6.3 蓝湖一面
说说之前的测试工作
为什么选择离职学前端而不是在之前公司寻求转岗
如何学习前端的
说说ajax和http（答得不太好）
http：超文本传输协议。服务端传送超文本信息到客户端的协议
特点：无状态：协议对于事物处理没有记忆能力。无连接：每次连接只处理一个请求，服务端发送完数据，收到客户端应答后，就断开连接。无连接的处理方式可以节约时间
http和https的区别
http信息是明文传输，https是具有安全性的加密传输协议
http和https连接方式不同，端口号也不同，http（80端口）https（443端口）
http无状态，无连接
https加密原理（没答上）
关键在与SSL层（安全套接字层），介于http层和tcp层之间，该层提供了身份验证和加密通讯方法
SSL层作用：
利用数字签名对服务端和客户端进行身份验证
利用对称密钥算法对传输的数据进行加密
使用相应算法来验证信息的完整性
编程题 有效的括号（说思路+代码实现+时间复杂度空间复杂度分析）时间空间复杂度答得不好
时间复杂度：程序中语句的执行条数，求出总的执行条数
空间复杂度：主要来自于程序运行过程中申请的临时储存空间

6.21 富途一面
vue组件传值
event bus细节
vuex响应式原理（没答上）
vue相关的全家桶用过哪些
tcp三次握手
了解闭包吗
同步和异步
原型链
编程题：括号匹配

6.28 神策数据一面
BigInt和Number区别
Number范围（正负2的53次方-1），BigInt超出该范围还是可以正常存储数据
简单数据类型和复杂数据类型区别
null和undefined区别

Number(null)=0   Number(undefined)=NaN
栈内存和堆内存区别
堆是动态分配内存，内存大小不一，也不会自动释放。栈是自动分配相对固定大小的内存空间，并由系统自动释放。
为什么会栈溢出，怎么解决栈溢出
为什么会栈溢出：
调用栈的内存空间是有限的，如果函数调用多次（递归），每次调用函数都会将数据加入栈中，数据过多就会占用大量的栈内存，就会导致栈溢出
如何实现对象深拷贝
如何实现function 深拷贝（不能用JSON.stringify()）
编程题：promise相关（没思路）
编程题：写一个函数，接受两个排好序的数组，将这两个数组合并，找出合并后数组的中位数（要求时间复杂度<=O(n)）（使用插入排序）

6.30 默安科技一面
实现浅拷贝深拷贝
浅拷贝：数组：1.slice；2.展开运算符；
对象：1.Object.assign()；2.扩展运算符
需要字体大小小于10px情况怎么处理（浏览器默认最小支持12px）
transform: scale(0.5);
js中数组为什么是对象类型
Array继承自Object，其本质是个对象，内部是key-value的存储方式（key是索引，value是数组元素）。这解释了js中的数组为什么可以存放不同类型的数据类型
为什么jsonp只支持get请求
为什么typeof null =object

7.1 默安科技二面
http各种状态码表示的信息
200 请求成功
301 请求的资源被永久移动到新url
400 客户端请求错误，服务器无法理解
401 未授权 请求要求用户身份认证
403 服务器拒绝执行该请求
404 无法找到资源
500 服务器内部错误，无法完成该请求
501 服务器不支持请求的功能

跨域方式及详细处理方式
客户端存储用户信息的方式及存储位置有什么不同
cookie
sessionStorage
localStorage
跨站请求伪造怎么实现的


7.6 杭州小影创新科技一面
如何实现多维数组扁平化（flat, reduce）
js判断是否为整数
1.math.floor(n)===n	2.Number.isInteger()
JSON.stringify实现深拷贝的局限
它能正确处理的对象只有 Number, String, Boolean, Array, 扁平对象，即那些能够被 json 直接表示的数据结构。
如何手写函数实现深拷贝（说下思路）
js如何添加异步任务进入（宏）任务队列
同步和异步任务分别进入不同的执行"场所"，同步的进入主线程，异步的进入Event Table并注册回调函数
当指定的事情完成时，Event Table会将这个函数移入Event Queue ，等待主线程的任务执行完毕
当栈中的代码执行完毕，执行栈（call stack）中的任务为空时，就会读取任务队列（Event quene）中的事件，去执行对应的回调
 如此循环，就形成js的事件循环机制（Event Loop）
宏任务和微任务
vue3 实现数据双向绑定proxy方法优势（可拦截/监听类型更多（13种））
可以直接代理对象而不是对象的属性（包括新增属性和删除属性），可以监听数组的变化
Object.defineProperty()缺点：
1.无法检测到对象属性的新增或删除；
2.不能监听数组变化
vue在实现数组的响应式时，它使用了一些hack，把无法监听数组的情况通过重写数组的部分方法来实现响应式，这也只限制在数组的push/pop/shift/unshift/splice/sort/reverse七个方法，其他数组方法及数组的使用则无法检测到


7.7 北京秒如科技一面
说说学校学过的课程
进程和线程
进程是操作系统资源调度的基本单位，线程是任务的调度执行的基本单位
线程是进程中的一部分，一个进程可以含有多个线程，但线程只能存在于一个进程中
进程间通信
管道pipe；命名管道FIFO；消息队列；共享存储；信号量；套接字，信号
计算机网络7层模型，tcp/ip四层模型

堆排序
b树和b+树
完全二叉树
完全二叉树的特点：叶子结点只能出现在最下层和次下层，且最下层的叶子结点集中在树的左部。

谈谈之前的测试工作项目
怎么理解渐进式渲染
提高网页性能，提高用户感知的加载速度，尽快呈现网页；
体现：
图片懒加载；
确定显示内容的优先级，先加载一部分必须的脚本，然后延迟加载剩余脚本；
异步加载HTML片段，当页面通过后台渲染时，把HTML拆分，通过异步请求分块发送给浏览器；
什么是优雅降级（渐进增强）
优雅降级：（向下兼容）一开始就构建站点的完整功能，然后针对浏览器测试和修复。比如一开始使用 CSS3 的特性构建了一个应用，然后逐步针对各大浏览器进行 hack 使其可以在低版本浏览器上正常浏览。
渐进增强：（向上兼容）一开始就针对低版本浏览器进行构建页面，完成基本的功能，然后再针对高级浏览器进行效果、交互、追加功能达到更好的体验
边缘计算
哪些app适合放到云上，哪些app不适合放到云上
go routine特点，和协程的区别
goroutine语言原生支持，不像其他编程语言需要其他库支持；
goroutine调度由go运行时管理；
当goroutine发生阻塞时，会让出让出cpu给其他goroutine；
goroutine非常轻量级，占用的内存开销非常小，创建一个goroutine的代价非常廉价，我们可以很轻松的创建上万个goroutine


7.8 上海再惠网络科技一面
没问什么难题
vuex原理（流程）
严格模式下没有直接调用者的函数this指向什么

7.16 牛客一面
BFC
闭包和作用域（闭包的本质）
事件循环细节
原型链题目
实现事件巴士（eventBus）-使用对象来实现（key是事件名，value是绑定的函数）

7.19牛客二面
v-if加在父元素上和子元素上有什么不同
正则：判断字符串是否是qq号
手写函数节流和防抖

7.22 阿里一面
为什么使用BigInt
直接改变数组长度会如何（数组里的元素会受何影响）
vue3 中proxy如何对一个很复杂对象实现响应式
快速创建一个元素为1到10的数组
let arr = new Array(10).fill(2).map((val, index) => index + 1);


onChain
清瞳时代
汉熵科技
时代浮沉

2021年11月：
北京数灵元科技
vue和其他框架的不同
交替打印红-绿-黄交通灯,红灯3秒,绿灯2秒,黄灯1秒
反转字符串

幂律智能


